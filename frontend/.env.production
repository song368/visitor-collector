# 前端【正式打包】环境变量
# 说明：Create React App 执行 npm run build（正式构建）时，会自动读取本文件。
# 这里把线上后端地址写进来，Vercel 构建时网页就会连到 Render 后端。
# （本文件是故意提交到 Git 的，和本地用的 .env 不同——避免新手在网页上找不到配置入口）
REACT_APP_API_URL=https://visitor-collector-trt0.onrender.com
