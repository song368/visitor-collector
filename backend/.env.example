# 后端环境变量示例（本地开发可复制为 .env；部署时由平台设置，不要提交真实密钥）
# 服务端口（本地默认 5000；Render 会自动注入 PORT，可不填）
PORT=5000

# 允许跨域的前端域名，多个用逗号分隔
# 本地：http://localhost:3000
# 生产：填你的 Vercel 前端域名，例如 https://your-frontend.vercel.app
CORS_ORIGINS=http://localhost:3000

# SQLite 数据库文件路径（可选，默认 backend/visitors.db）
# DB_PATH=/path/to/visitors.db
