# 网站搭建与部署 · 学习提示词集

> 这是一套**可直接复制给 AI（WorkBuddy / ChatGPT 等）**的提问模板，按学习路径分模块组织。
> 配套示例项目：`visitor-collector`（访客信息收集表，React + Flask + SQLite）。
> 用法：把 `[占位符]` 换成你的实际情况，整段发给 AI 即可。建议**一个模块一个模块地吃透**，别一次问完。

---

## 模块一：建立全局认知（先搞懂“为什么这么分”）

**1.1 用一句话讲清前后端分离**
> 我在学 Web 开发，请用一个生活化的比喻，向我解释什么是“前后端分离”。再说明：前端负责什么、后端负责什么、数据库在什么位置。最后用我这个示例项目（React 前端 + Flask 后端 + SQLite）对应说明每一部分落在哪个文件里。

**1.2 看懂项目结构**
> 这是我刚拿到的全栈项目目录结构（贴出目录树）。请逐层告诉我：每一类文件是干什么的？一个“用户提交表单 → 数据入库 → 后台展示”的请求，从前端到数据库再回到前端，完整经过了哪些文件、按顺序是什么？

**1.3 技术选型答疑**
> 为什么示例项目选了 React + Flask + SQLite 这套组合给初学者？分别有什么优缺点？如果我想做“能上生产”的版本，每一层应该换成什么？

---

## 模块二：前端 React 学习

**2.1 读懂这段代码**
> 下面是一段 React 组件代码（贴 `VisitorForm.js`）。请逐行用大白话解释：① `useState` 在这里存了什么；② 为什么用 `name` 属性做统一 `onChange`；③ `e.preventDefault()` 在防什么；④ 提交成功后为什么要 `setForm(...)` 清空。让我能照着改出“增加字段”的功能。

**2.2 表单校验怎么做**
> 我想给表单加一个“年龄”字段，要求 1–120 的整数，且必填。请基于现有 `validate()` 函数，告诉我怎么改 `VisitorForm.js`，并把修改后的完整函数给我，带注释。

**2.3 路由与多页面**
> 解释 `react-router-dom` 的 `BrowserRouter` / `Routes` / `Route` 分别是什么。如果我要再加一个 `/about` 关于页面，需要改哪几个文件、加什么代码？请给完整示例。

**2.4 环境变量在前端怎么用**
> CRA 里 `process.env.REACT_APP_API_URL` 是什么？为什么变量名必须以 `REACT_APP_` 开头？`.env` 和 `.env.example` 的区别是什么？改了 `.env` 后为什么要重启 `npm start`？

---

## 模块三：后端 Flask 学习

**3.1 读懂后端代码**
> 这是我的 Flask 后端 `app.py`。请解释：① `@app.route` 装饰器的作用；② `request.get_json()` 拿到了什么；③ 为什么 GET 和 POST 能共用 `/api/visitors` 这个路径；④ 返回 `jsonify(...)` 和 `201` 状态码的含义。让我能照着加一个“删除某条记录”的接口。

**3.2 写一个删除接口**
> 请基于现有 `app.py`，帮我写 `DELETE /api/visitors/<id>` 接口：从 SQLite 删除指定 id 的记录，并返回成功/未找到的对应状态码。给出完整代码和注释。

**3.3 为什么后端也要校验**
> 前端已经校验过手机号和邮箱了，为什么 `add_visitor` 里还要再校验一遍？请用“有人绕过前端直接调接口”的场景说明风险，并列出后端校验的最佳实践清单。

---

## 模块四：数据库（SQLite）

**4.1 表结构是怎么来的**
> 解释 `app.py` 里 `CREATE TABLE IF NOT EXISTS visitors (...)` 这段 SQL：每个字段类型什么意思？`PRIMARY KEY AUTOINCREMENT` 和 `DEFAULT CURRENT_TIMESTAMP` 各有什么作用？`IF NOT EXISTS` 为什么重要？

**4.2 手动查看数据库**
> 我想不写代码、直接看 `visitors.db` 里现在有哪些数据，应该用哪个命令行工具（sqlite3）？给我从安装到执行一条 `SELECT * FROM visitors;` 的完整步骤（Windows 和 macOS 分别说明）。

**4.3 从 SQLite 迁移到 PostgreSQL**
> 如果以后访问量大了，要把 SQLite 换成 PostgreSQL，后端代码（连接、建表、SQL 语句）大概要改哪些地方？给我一个改造清单，不用写全部代码，说清“改什么、为什么”。

---

## 模块五：前后端联调与 CORS

**5.1 CORS 到底是什么**
> 我用前端 `localhost:3000` 调后端 `localhost:5000`，浏览器报 CORS 错误。请用大白话解释：① 什么是同源策略；② 为什么前后端端口不同就算“跨域”；③ `Flask-CORS` 的 `origins` 参数在允许什么。再用我的 `config.py` + `CORS(...)` 代码说明现在允许了谁。

**5.2 排错清单**
> 给我一份“前端调不通后端”的排错清单，按出现概率排序，覆盖：CORS、地址写错、后端没启动、环境变量没生效、HTTPS/HTTP 混用等，每条给出“现象 + 怎么查 + 怎么修”。

---

## 模块六：环境变量与配置管理

**6.1 理解环境变量**
> 请用初学者能懂的话解释“环境变量”：它存在哪里、程序怎么读、和把值直接写死在代码里相比好在哪？结合我的项目，列出前端和后端各自有哪些环境变量、分别在哪读。

**6.2 不同环境如何切换**
> 我的项目在“本地”和“线上”要连不同地址。请说明一套干净的变量管理方案：本地用什么文件、部署平台用什么方式设置、哪些变量**绝不能**提交进 Git（如含密钥的）。给我一个 `.gitignore` 的推荐内容（针对本项目）。

---

## 模块七：部署（Render / Vercel）

**7.1 部署前准备**
> 我要把示例项目部署上线。请先告诉我部署前必须确认的几件事（环境变量、构建命令、启动命令、目录结构），再分别给出 Render（后端）和 Vercel（前端）的**分步操作清单**，每一步说清在网站哪个位置点什么。

**7.2 读懂部署配置**
> 解释后端这几个部署文件各自的作用：`requirements.txt`、`Procfile`、`runtime.txt`。如果 `gunicorn app:app` 里的 `app` 指的是什么？如果我把后端文件改名了，这句要怎么改？

**7.3 部署失败怎么查**
> 列出 Render / Vercel 部署最常见的 5 种失败原因及排查方法（构建失败、启动命令错、环境变量缺失、端口问题、CORS），并告诉我去哪里看日志。

---

## 模块八：实战排错与复盘

**8.1 遇到报错怎么问 AI 最有效**
> 当我运行项目报错时，怎样把信息整理给 AI 才能最快得到准确答案？给我一个“报错求助模板”，包含：环境、做了什么操作、完整报错信息、已尝试过的办法。并示范一次（用你之前遇到的某个真实报错）。

**8.2 项目复盘**
> 我已经把这个访客收集项目跑通并部署上去了。请基于以下信息（贴出你做了什么、踩了哪些坑）帮我做一次复盘：① 哪些概念现在真正理解了；② 哪一步最卡、为什么；③ 下一步该学什么能衔接上“真实生产项目”（鉴权、数据库迁移、日志、监控等）。

**8.3 给我出个小测验**
> 请基于这个示例项目出 10 道选择题/简答题，覆盖：前后端分离、React 状态、Flask 路由、SQLite、CORS、环境变量、部署。难度从易到难，并附参考答案，帮我检验自己是否真学会了。

---

### 使用建议
1. **顺序学**：模块一 → 二/三/四（基础）→ 五/六（联调与配置）→ 七（部署）→ 八（复盘）。
2. **带着代码问**：把对应文件内容贴给 AI，比空口问更准。
3. **改一改再问**：每个模块学完，试着按提示改一点代码（加字段、加接口），跑不通就回到对应模块追问。
